/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "source.unsplash.com",
      "abyssinian-aeolian-gazelle.glitch.me",
    ],
  },
};

export default nextConfig;
